import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";
test("voice startup sends no agent context before the user speaks",async()=>{const app=await readFile(new URL("../public/app.js",import.meta.url),"utf8");const startup=app.slice(app.indexOf("async function startElevenAgent"),app.indexOf("async function stopElevenAgent"));assert.doesNotMatch(startup,/sendContextualUpdate/);assert.doesNotMatch(startup,/dynamicVariables/);assert.match(app,/syncUserMessageWithAidBay/)});
test("an ElevenLabs failure keeps the conversation open with a local fallback",async()=>{const app=await readFile(new URL("../public/app.js",import.meta.url),"utf8");assert.match(app,/if \(elevenConnectionFailed\) \{\s*activateLocalVoiceFallback\(\)/);assert.match(app,/function activateLocalVoiceFallback\(\)/);assert.match(app,/Listening with the backup microphone/);assert.match(app,/Voice is unavailable in this browser\. Type your request below/)});
